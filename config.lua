local _, ns = ...
local C = ns.C

-- ----------------------------------------------------------
-- > Your Config here (will override the defaults settings)
-- ----------------------------------------------------------
-- Important: Override each property individually
